# Leave API URL unset for Vite's local proxy.
# For a separately hosted static build, point it to the Gateway explicitly.
# VITE_API_BASE_URL=http://localhost:3000/api/v1

# Leave unset in local development so Socket.IO is proxied to tracking-service.
# VITE_TRACKING_WS_URL=http://localhost:3004
